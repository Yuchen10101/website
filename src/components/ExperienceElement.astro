---
// ExperienceElement.astro
const { company, href, imageSrc, location, department, position, duration } = Astro.props;
---

<div class="experience-element">
    <div class="image">
        <img src={imageSrc} alt="PaperImg" />
    </div>
    <div class="content">
        <p>
            <a href={href}>
                <strong>{company}</strong>
            </a>
            - {location}
            <br>
            <br>
            {department}, {position}
            <br>
            <em>{duration}</em>
        </p>
    </div>
</div>

<style>
    .experience-element {
        position: relative;
        display: flex;
        overflow: hidden;
        width: 100%; 
        max-width: 1000px;
        margin-left: 30px;
        margin-right: auto;
        margin-top: 20px;
        margin-bottom: 20px;
        transition: transform 0.3s ease-in-out;
    }

    .experience-element:hover {
        transform: translateY(-5px);
    }

    .image {
        position: absolute;
        left: 10px;
        top: 0;
        height: 100%;
    }

    .image img {
        height: 90%;
        width: auto;
        display: block;
    }

    .content {
        padding: 20px;
        text-align: left; 
        margin-left: 180px; 
        margin-bottom: 30px;
    }

    .content a {
        font-size: 1.3rem;
        text-decoration: none;
        color: inherit;
    }

    .content a:hover {
        text-decoration: underline;
    }

    .content p {
        margin: 0;
        font-size: 1.15rem;
        color: rgba(0,0,0,0.9);
    }
</style>